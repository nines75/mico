name: test

on:
    push:
        branches:
            - main
    pull_request:

jobs:
    tsc:
        runs-on: ubuntu-latest

        steps:
            - uses: nines75/composite-action/setup-pnpm@main

            - name: tsc
              run: tsc --noEmit
